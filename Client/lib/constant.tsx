export const ACCESS_TOKEN = "access_token"; // this is used to store the access token in the local storage
export const REFRESH_TOKEN = "refresh_token"; // these name are standard in OAuth2-based APIs (like Google, Facebook, etc.)
// Are often used in OAuth2-based APIs (like Google, Facebook, etc.), where "access_token" is a standard parameter name.
